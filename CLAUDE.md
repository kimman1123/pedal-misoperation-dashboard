# 작업 지침

## HTML 대시보드 업로드 규칙

사용자가 index 파일(HTML 대시보드)을 제공하면 다음 절차에 따라 이 저장소에 업로드한다.

1. **파일명은 항상 `index.html`로 통일**한다. 사용자가 제공한 파일명이 다르더라도 저장소에 커밋할 때는 반드시 `index.html`로 저장한다.
2. **업로드 대상 저장소**: https://github.com/kimman1123/pedal-misoperation-dashboard.git (`main` 브랜치)
3. 커밋 전에 `git fetch origin`으로 원격 브랜치와의 차이를 확인한다. 원격이 앞서 있으면(예: GitHub 웹에서 직접 업로드된 커밋이 있는 경우) 먼저 로컬을 원격과 동기화한 뒤, 제공받은 파일을 반영해서 커밋·푸시한다.
4. 디자인(레이아웃/CSS/스크립트 구조)은 별도 지시가 없는 한 그대로 유지하고, 데이터 관련 변경만 반영한다.
